# Dev-only Dockerfile — hot reload via Vite dev server
FROM node:22-alpine

WORKDIR /app

COPY package.json ./
RUN npm install

COPY . .

EXPOSE 8080

# --host 0.0.0.0 makes Vite listen on all interfaces inside Docker
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "8080"]
